
import accepted from "./accepted.js";
import dismissed from "./dismissed.js";
import installPrompt from "./installPrompt.js";
import appInstalled from "./appInstalled.js";
import appUninstalled from "./appUninstalled.js";

const app = {
    accepted: accepted,
    dismissed: dismissed,
    installPrompt: installPrompt,
    appInstalled: appInstalled,
    appUninstalled: appUninstalled
};

export default app;
